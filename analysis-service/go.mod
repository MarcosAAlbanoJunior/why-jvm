// Servico de analise (lado Go do split — Fase 5). Recebe o IncidentRecord JSON
// que o lado Java extrai do JFR, persiste e (B2+) serve as tools MCP / agente.
//
// Caminho do modulo e um placeholder ate publicar; ajuste ao repo real.
module github.com/whyjvm/analysis-service

go 1.23
